/* FINAL_UI_V13 — dashboard logic, fully local and offline-safe */
const PROFILE_KEY='samed-profile-v1',PROGRESS_KEY='samed-offline-progress-v1',INSTALLED_KEY='samed-installed-units-v1';
const gradeNames={6:'السادس',7:'السابع',8:'الثامن',9:'التاسع',10:'العاشر',11:'الحادي عشر',12:'الثاني عشر'};
const plans={
 physics:{6:['القياس والوحدات','الحركة والسرعة','القوى من حولنا','الشغل والطاقة','الضوء والرؤية','الصوت'],7:['المادة وحالاتها','الحرارة ودرجة الحرارة','الآلات البسيطة','الكهرباء الساكنة','المغناطيسية'],8:['الحركة المنتظمة','القوة والاحتكاك','الضغط والكثافة','الطاقة وتحولاتها','الدارات الكهربائية'],9:['الحركة بتسارع ثابت','قوانين نيوتن','الشغل والقدرة','الموجات','التيار الكهربائي'],10:['المتجهات','الحركة في بعدين','قوانين نيوتن وتطبيقاتها','الشغل والطاقة والقدرة','مقدمة في الزخم'],11:['الحركة الرأسية والمقذوفات','الحركة الدائرية','الجاذبية الكونية','الموائع','الاهتزاز والموجات'],12:['الزخم الخطي والدفع','الكهرباء الساكنة','التيار والدارات','المجال المغناطيسي','الحث الكهرومغناطيسي','مقدمة الفيزياء الحديثة']},
 chemistry:{6:['المادة وخواصها','المخاليط والمحاليل','التغيرات الفيزيائية والكيميائية','الماء ودورته'],7:['بناء الذرة','العناصر والمركبات','مقدمة الجدول الدوري','الأحماض والقواعد حولنا'],8:['التركيب الذري والإلكترونات','الروابط الكيميائية','التفاعلات الكيميائية','المحاليل والذائبية'],9:['المعادلات الكيميائية ووزنها','الأحماض والقواعد والأملاح','الأكسدة والاختزال','مقدمة الكيمياء العضوية'],10:['المول والحسابات الكيميائية','الجدول الدوري والدورية','أنواع الروابط','الغازات وقوانينها'],11:['الاتزان الكيميائي','سرعة التفاعل','الأحماض والقواعد وحساب pH','الكهروكيمياء'],12:['البناء الإلكتروني للذرة','الكيمياء الحرارية','الكيمياء العضوية','البوليمرات','التحليل الكيميائي']}
};
const stageCatalog={
 physics:{unitId:'physics12-momentum',unitTitle:'الزخم الخطي والدفع',stages:[
  {icon:'🧱',title:'مدخل تأسيسي',summary:'مراجعة اختيارية لمهارات الصفين 10 و11',key:'physics12-foundation-momentum',total:8,href:'physics-foundation.html',optional:true},
  {icon:'🧭',title:'مراجعة الوحدة',summary:'افهم مفاهيم الزخم والدفع بهدوء',key:'physics12-review-momentum',total:7,href:'physics-review.html'},
  {icon:'📘',title:'تمارين الكتاب',summary:'طبّق على أسئلة الكتاب خطوةً خطوة',key:'physics12-textbook-momentum',total:10,href:'physics-textbook.html'},
  {icon:'🎯',title:'تدريب إضافي',summary:'ثبّت الفهم بأسئلة جديدة',key:'physics12-momentum',total:15,href:'unit.html'}]},
 chemistry:{unitId:'chemistry12-atomic-structure',unitTitle:'البناء الإلكتروني للذرة',stages:[
  {icon:'🧱',title:'مدخل تأسيسي',summary:'مراجعة اختيارية للذرة من الصفين 10 و11',key:'chemistry12-foundation-atom',total:8,href:'chemistry-foundation.html',optional:true},
  {icon:'🧭',title:'مراجعة الوحدة',summary:'ابنِ المفاهيم والعلاقات الأساسية',key:'chemistry12-review-unit1',total:8,href:'chemistry-review.html'},
  {icon:'📘',title:'تمارين الكتاب',summary:'حل تمارين الكتاب بخطوات كاملة',key:'chemistry12-textbook-unit1',total:20,href:'chemistry-textbook.html'},
  {icon:'🎯',title:'تدريب إضافي',summary:'ثبّت الفهم بتدريب متدرج',key:'chemistry12-atomic-structure',total:15,href:'chemistry-unit.html'}]}
};
const unitFiles={
 'physics12-momentum':['./physics-hub.html','./physics-foundation.html','./physics-review.html','./physics-textbook.html','./unit.html','./review.js','./foundation.js','./review.css','./unit.js','./unit-physics-foundation-data.js','./unit-physics-review-data.js','./unit-physics-textbook-data.js','./unit-momentum-data.js','./styles.css','./dashboard-v13.css','./icon-192.png'],
 'chemistry12-atomic-structure':['./chemistry-hub.html','./chemistry-foundation.html','./chemistry-review.html','./chemistry-textbook.html','./chemistry-unit.html','./review.js','./foundation.js','./review.css','./unit.js','./unit-chemistry-foundation-data.js','./unit-chemistry-review-data.js','./unit-chemistry-textbook-data.js','./unit-chemistry-data.js','./styles.css','./dashboard-v13.css','./icon-192.png']
};
const unitPages={'physics12-momentum':'physics-hub.html','chemistry12-atomic-structure':'chemistry-hub.html'};
const unitVersions={'physics12-momentum':'2.0.0-ui18','chemistry12-atomic-structure':'2.0.0-ui18'};
let profile=null,currentSubject='physics',moduleFilter='all',deferredPrompt=null;
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
function read(k,d){try{const value=JSON.parse(localStorage.getItem(k));return value??d}catch(e){return d}}
function save(k,v){try{localStorage.setItem(k,JSON.stringify(v))}catch(e){}}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function toast(message){const box=$('#toast');box.textContent=message;box.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>box.classList.remove('show'),2400)}
function normalizeProfile(p){if(!p)return null;const map={phys:'physics',physics:'physics',chem:'chemistry',chemistry:'chemistry'};p.subjects=[...new Set((p.subjects||[]).map(x=>map[x]).filter(Boolean))];if(!p.subjects.length)p.subjects=['physics'];return p}
function network(){const online=navigator.onLine;$('#net').textContent=online?'متصل':'دون إنترنت';$('#net').classList.toggle('offline',!online)}
addEventListener('online',network);addEventListener('offline',network);network();

async function passwordDigest(value){try{if(crypto&&crypto.subtle){const bytes=new TextEncoder().encode(value);const digest=await crypto.subtle.digest('SHA-256',bytes);return Array.from(new Uint8Array(digest)).map(x=>x.toString(16).padStart(2,'0')).join('')}}catch(e){}let h=2166136261;for(let i=0;i<value.length;i++){h^=value.charCodeAt(i);h=Math.imul(h,16777619)}return 'local-'+(h>>>0).toString(16)}
async function profileFromForm(secretHash){const subjects=[];if($('#physics').checked)subjects.push('physics');if($('#chemistry').checked)subjects.push('chemistry');return{id:(profile&&profile.id)||('local-'+Date.now()),name:$('#name').value.trim(),grade:+$('#grade').value,subjects,passwordHash:secretHash||(profile&&profile.passwordHash)||'',createdAt:(profile&&profile.createdAt)||new Date().toISOString(),mode:'local'}}
$('#profileForm').addEventListener('submit',async event=>{event.preventDefault();const password=$('#password').value,confirm=$('#confirmPassword').value,hasHash=!!(profile&&profile.passwordHash);if($('#name').value.trim().length<2)return toast('اكتب اسمًا من حرفين على الأقل');if(!$('#physics').checked&&!$('#chemistry').checked)return toast('اختر مادة واحدة على الأقل');if(!hasHash||password||confirm){if(password.length<6)return toast('اكتب كلمة مرور من 6 أحرف على الأقل');if(password!==confirm)return toast('كلمة المرور وتأكيدها غير متطابقين')}const secretHash=password?await passwordDigest(password):(profile&&profile.passwordHash)||'';const p=await profileFromForm(secretHash);profile=p;save(PROFILE_KEY,p);$('#password').value='';$('#confirmPassword').value='';showDashboard()});

function progressPercent(key){const all=read(PROGRESS_KEY,{}),entry=all[key]||{};const value=Number(entry.percent||0);return Math.max(0,Math.min(100,Number.isFinite(value)?value:0))}
function subjectStats(subject){const data=stageCatalog[subject];if(!data)return{done:0,total:0,pct:0};let done=0,total=0;for(const stage of data.stages){if(stage.optional)continue;const pct=progressPercent(stage.key);done+=stage.total*pct/100;total+=stage.total}return{done:Math.round(done),total,pct:total?Math.round(done*100/total):0}}
function selectedLiveSubjects(){return profile&&profile.grade===12?profile.subjects.filter(x=>stageCatalog[x]):[]}
function overallStats(){const subjects=selectedLiveSubjects();const selected=subjects.includes(currentSubject)?currentSubject:subjects[0];if(!selected)return{done:0,total:0,pct:0,stages:3,xp:0};const stats=subjectStats(selected);return{done:stats.done,total:stats.total,pct:stats.pct,stages:3,xp:stats.done?stats.done*100+50:0}}
function paintMetrics(){const stats=overallStats();$('#heroRing').style.setProperty('--progress',stats.pct);$('#heroPct').textContent=stats.pct+'%';$('#heroProgressMeta').textContent=stats.done+' من '+stats.total+' محتوى';$('#doneMetric').textContent='أنجزت '+stats.done+' محتوى';$('#stageMetric').textContent=(stats.stages||3)+' مراحل + تأسيس اختياري';$('#xpMetric').textContent=stats.xp+' نقطة'}

function renderPathFilters(){const subjects=selectedLiveSubjects();const box=$('#pathFilters');if(!subjects.length){box.innerHTML='<span class="path-filter active">المسار قيد الإعداد</span>';return}if(!subjects.includes(currentSubject))currentSubject=subjects[0];box.innerHTML=subjects.map(sub=>`<button class="path-filter ${sub===currentSubject?'active':''}" data-path="${sub}">${sub==='physics'?'⚛️ الفيزياء':'🧪 الكيمياء'}</button>`).join('');box.querySelectorAll('[data-path]').forEach(btn=>btn.onclick=()=>{currentSubject=btn.dataset.path;renderPathFilters();paintMetrics();renderTimeline();renderModules()})}
function renderTimeline(){const box=$('#timeline'),catalog=stageCatalog[currentSubject];if(!catalog||profile.grade!==12){box.innerHTML='<div class="stage-card is-active"><span class="stage-node">🛠️</span><h3>المسار قيد الإعداد</h3><p>سيظهر هنا فور إضافة محتوى صفك.</p></div>';return}const pcts=catalog.stages.map(s=>progressPercent(s.key));let activeOffset=pcts.slice(1).findIndex(v=>v<100),active=activeOffset<0?pcts.length-1:activeOffset+1;box.innerHTML=catalog.stages.map((stage,index)=>{const pct=pcts[index],optional=!!stage.optional,done=pct>=100,isActive=index===active,state=done?'مكتمل':optional?'اختياري · متاح':isActive?'مقترح الآن · متاح':'متاح للدخول';const label=done?'مراجعة الجزء':optional?'فتح التأسيس':'فتح هذا الجزء';return `<article class="stage-card ${optional?'is-optional':''} ${done?'is-done':''} ${isActive?'is-active':''}"><span class="stage-node">${stage.icon}</span><small>${optional?'دعم اختياري':'الجزء '+index}</small><h3>${stage.title}</h3><p>${stage.summary}</p><div class="stage-progress"><i style="width:${pct}%"></i></div><div class="stage-meta"><span>${state}</span><b>${pct}%</b></div><a class="stage-cta ${optional?'optional':''}" href="${stage.href}">${label} ←</a></article>`}).join('')}

function featuredModules(){if(profile.grade!==12){const out=[];for(const sub of profile.subjects){(plans[sub][profile.grade]||[]).slice(0,3).forEach((name,index)=>out.push({subject:sub,name,index,ready:false}))}return out}const out=[];if(profile.subjects.includes('physics')){out.push({subject:'physics',name:'الزخم الخطي والدفع',index:0,ready:true,id:'physics12-momentum'});out.push({subject:'physics',name:'الكهرباء الساكنة',index:1,ready:false})}if(profile.subjects.includes('chemistry'))out.push({subject:'chemistry',name:'البناء الإلكتروني للذرة',index:0,ready:true,id:'chemistry12-atomic-structure'});return out}
function unitPct(item){return item.ready?subjectStats(item.subject).pct:0}
function moduleDescription(item){if(item.id==='physics12-momentum')return 'المسار الرئيسي: مراجعة الوحدة، ثم تمارين الكتاب، ثم التدريب؛ والتأسيس متاح اختياريًا عند الحاجة.';if(item.id==='chemistry12-atomic-structure')return 'مراجعة الوحدة ثم تمارين الكتاب والتدريب، مع تأسيس اختياري من السنوات السابقة.';if(item.name==='الكهرباء الساكنة')return 'القوة الكهربائية والمجال والجهد؛ ستُضاف وفق المسار المتدرج نفسه.';return 'وحدة مسجلة ضمن البرنامج السنوي لهذا الصف.'}
function renderModules(){const installed=read(INSTALLED_KEY,{});const items=featuredModules().filter(item=>moduleFilter==='all'||item.subject===moduleFilter);$('#modulesSubtitle').textContent='محتوى الصف '+gradeNames[profile.grade]+' · افتح البطاقة للاطلاع على التفاصيل';$('#moduleList').innerHTML=items.map((item,index)=>{const pct=unitPct(item),isInstalled=item.id&&installed[item.id],subjectLabel=item.subject==='physics'?'فيزياء':'كيمياء',icon=item.subject==='physics'?'⚛️':'🧪';const tags=item.ready?`<span class="ui-tag ready">متاحة الآن</span><span class="ui-tag ${item.subject==='physics'?'active':'review'}">${item.subject==='physics'?'مسار علاجي':'مراجعة موجهة'}</span>${isInstalled?'<span class="ui-tag ready">✓ محمّلة</span>':''}`:'<span class="ui-tag soon">قيد الإعداد</span>';const actions=item.ready?`<div class="module-actions"><button class="ui-control primary" data-install="${item.id}">${isInstalled?'تحديث الوحدة':'تحميل الوحدة'}</button><a class="ui-control" href="${unitPages[item.id]}">فتح المسار ←</a></div>`:'<div class="module-actions"><button class="ui-control" disabled>ستتاح قريبًا</button></div>';return `<details class="module-card ${item.subject}" ${index===0?'open':''}><summary><span class="module-icon">${icon}</span><span class="module-copy"><h3>${esc(item.name)}</h3><p>${subjectLabel} · الوحدة ${item.index+1}</p></span><span class="module-tags">${tags}</span><span class="mini-ring" style="--progress:${pct}"><b>${pct}%</b></span></summary><div class="module-body"><p>${moduleDescription(item)}</p>${actions}</div></details>`}).join('')||'<div class="module-note">لا توجد وحدات ضمن المرشح الحالي.</div>';$$('[data-install]').forEach(button=>button.onclick=()=>installUnit(button.dataset.install,button))}
$$('.module-filter').forEach(button=>button.onclick=()=>{moduleFilter=button.dataset.filter;$$('.module-filter').forEach(x=>x.classList.toggle('active',x===button));renderModules()});

async function installUnit(id,button){if(!('caches'in window))return toast('التخزين غير مدعوم في هذا المتصفح');const original=button.textContent;button.disabled=true;button.textContent='جارٍ التحميل…';try{const cache=await caches.open('samed-units-v18');const files=unitFiles[id]||[];for(const file of files)await cache.add(file);const installed=read(INSTALLED_KEY,{});installed[id]={version:unitVersions[id]||'2.0.0',at:new Date().toISOString()};save(INSTALLED_KEY,installed);toast('اكتمل تحميل الوحدة للعمل دون اتصال');renderModules();button.textContent='تم التحميل ✓'}catch(error){toast('تعذر التحميل. تحقق من الاتصال وحاول مجددًا');button.disabled=false;button.textContent=original}}

function showDashboard(){if(!profile)return;$('#auth').classList.add('hidden');$('#dashboard').classList.remove('hidden');$$('.dashboard-only').forEach(x=>x.classList.remove('hidden'));$('#welcome').textContent='مرحبًا '+profile.name+' 👋';$('#gradeMetric').textContent='Grade '+profile.grade+' · الصف '+gradeNames[profile.grade];const live=selectedLiveSubjects();if(!live.includes(currentSubject))currentSubject=live[0]||profile.subjects[0]||'physics';paintMetrics();renderPathFilters();renderTimeline();renderModules()}
function showProfileEditor(){profile=normalizeProfile(read(PROFILE_KEY,null));if(!profile)return;$('#name').value=profile.name||'';$('#grade').value=profile.grade||12;$('#physics').checked=profile.subjects.includes('physics');$('#chemistry').checked=profile.subjects.includes('chemistry');$('#password').value='';$('#confirmPassword').value='';$('#dashboard').classList.add('hidden');$('#auth').classList.remove('hidden');$$('.dashboard-only').forEach(x=>x.classList.add('hidden'));scrollTo({top:0,behavior:'smooth'})}
$('#editProfile').onclick=showProfileEditor;const footerEdit=$('#footerEdit');if(footerEdit)footerEdit.onclick=event=>{event.preventDefault();showProfileEditor()};
$('#downloadUnit').onclick=event=>{const catalog=stageCatalog[currentSubject]||stageCatalog[selectedLiveSubjects()[0]];if(!catalog)return toast('ستظهر الوحدة القابلة للتحميل عند توفرها');installUnit(catalog.unitId,event.currentTarget)};
addEventListener('beforeinstallprompt',event=>{event.preventDefault();deferredPrompt=event;$('#install').classList.remove('hidden')});
$('#install').onclick=()=>deferredPrompt?deferredPrompt.prompt():toast('من قائمة المتصفح اختر: إضافة إلى الشاشة الرئيسية');
if('serviceWorker'in navigator&&location.protocol!=='file:')navigator.serviceWorker.register('./service-worker.js').catch(()=>{});
profile=normalizeProfile(read(PROFILE_KEY,null));if(profile){save(PROFILE_KEY,profile);showDashboard()}
